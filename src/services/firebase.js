/**
 * Placeholder Firebase init: in a real app, import firebase/app and firebase/firestore,
 * then initialize with config provided in Settings.
 * We keep it minimal here to avoid bundling heavy SDK unless requested.
 */
export function initFirebase(config) {
  // Return null; integrate Firebase SDK later as needed.
  return null;
}